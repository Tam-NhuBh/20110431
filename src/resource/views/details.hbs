<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Details</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.5.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .container {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }
        .card {
            margin-top: 20px;
        }
        .comment-card {
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="card">
            <div class="card-body">
                <h1 class="card-subtitle mb-2 text-muted">Bài viết số {{blog.id}}</h1>
                <p class="card-text">{{blog.content}}</p>
            </div>
        </div>

        <div class="mt-4">
            <h2>Thêm bình luận</h2>
            <form method="POST" action="/details/{{blog.id}}">
                <div class="mb-3">
                    <label for="comment" class="form-label">Bình luận của bạn</label>
                    <textarea id="comment" name="comment" class="form-control" rows="4" required></textarea>
                </div>
                <button type="submit" class="btn btn-primary">Gửi</button>
            </form>
        </div>

        <div class="mt-4">
            <h2>Bình luận</h2>
            <div class="card">
                <ul class="list-group list-group-flush">
                    {{#each blog.comments as |comment user|}}
                    <li class="list-group-item comment-card">
                        <strong>{{user}}</strong>
                        <p>{{comment}}</p>
                    </li>
                    {{/each}}
                </ul>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.5.0/dist/js/bootstrap.min.js"></script>
</body>
</html>
